from typing import AwaitableGenerator


x = [1, 2, 3]
y = [2, 5, 1]

def lagrange(x, y):
    #Eq do polinomio de lagrange
    if len(x) == len(y):
        a 
    else:
        raise TypeError('O número de coordenadas x é diferente do número de coordenadas y')


def subs(x):
    #O valor do polinomio de lagrange num ponto x